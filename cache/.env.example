# Deployment Settings
# Environment type (e.g., dev-, prod-)
ENV_TYPE=dev-
# Environment suffix (e.g., -0, -1)
ENV_SUFFIX=-0
# Path to Tailscale auth key file
TSAUTHKEY_PATH=./tsauthkey.key
# Set to true if tsauthkey is managed externally
TSAUTHKEY_EXTERNAL=false

# Valkey Core Settings
# Port for Valkey service (default: 6379)
VALKEY_PORT=6379
# Password for client authentication (sets 'requirepass' directive)
# VALKEY_PASSWORD=your_secure_password
# Password for valkey-cli authentication (alternative to VALKEY_PASSWORD)
# REDISCLI_AUTH=your_secure_password

# Valkey Memory Management
# Maximum memory limit (e.g., 256mb, 1gb)
# VALKEY_MAXMEMORY=256mb
# Memory eviction policy (e.g., volatile-lru, allkeys-lru, noeviction)
# VALKEY_MAXMEMORY_POLICY=noeviction

# Valkey Replication Settings
# Replication mode (master or slave)
# VALKEY_REPLICATION_MODE=master
# Master node host for replication (if in slave mode)
# VALKEY_MASTER_HOST=your_master_host
# Master node port for replication (default: 6379)
# VALKEY_MASTER_PORT=6379